import { Grid } from "@mui/material";
import React from "react";

const Home: React.FC = () => {
  return (
    <Grid container spacing={2}>
      <Grid item spacing={2} container xs={12}>
        Exercícios de CRUD usando Typescript, criação de páginas, routes,
        componentização com ReactJS, estilização com Material UI, Redux Toolkit
        Adapter e consumo de API.
      </Grid>
    </Grid>
  );
};

export default Home;
